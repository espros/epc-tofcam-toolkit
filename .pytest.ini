[pytest]
testpaths = tests
markers =
    systemTest: test marked as system will need a camera.
    manualTest: test marked as manual will not be carried out in CI/CD pipeline, etc.
addopts = -m "not manualTest"